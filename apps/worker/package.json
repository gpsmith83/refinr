{
  "name": "@refinr/worker",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "node -e \"console.log('worker: no tests yet (B-008 will add)')\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
