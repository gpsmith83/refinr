{
  "name": "@refinr/api",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "node -e \"console.log('api: no tests yet (B-002 will add)')\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
